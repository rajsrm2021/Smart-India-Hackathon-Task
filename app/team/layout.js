"use client";

import Navbar from "@/components/navbar";

const TeamLayout = ({ children }) => {
  return (
    // <html>
    //   <body>{children}</body>
    // </html>
    <div>{children}</div>
  );
};

export default TeamLayout;
